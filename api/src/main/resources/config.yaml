kumuluzee:
  name: listings-service
  env:
    name: dev
  version: 1.0.0
  server:
    base-url: http://localhost:8080
    http:
      port: 8080
  datasources:
    - jndi-name: jdbc/ListingsDS
      connection-url: jdbc:postgresql://pg-listings:5432/listings
      username: dbuser
      password: postgres
  config:
    consul:
      agent: http://consul:8500